// Stub for the platform-wide upgrade-prompt UI. The real implementation
// will live in @dhc/shared once that package ships; until then, render
// nothing so tier-gated UI quietly disappears.

export const UpgradePrompt = () => null;

export default UpgradePrompt;
